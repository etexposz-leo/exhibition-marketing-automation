version: '3.8'

services:
  app:
    build: .
    container_name: exhibition-marketing-app
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/marketing.db
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - LINKEDIN_ACCESS_TOKEN=${LINKEDIN_ACCESS_TOKEN:-}
      - FACEBOOK_ACCESS_TOKEN=${FACEBOOK_ACCESS_TOKEN:-}
      - INSTAGRAM_ACCESS_TOKEN=${INSTAGRAM_ACCESS_TOKEN:-}
      - X_BEARER_TOKEN=${X_BEARER_TOKEN:-}
      - GOOGLE_BUSINESS_API_KEY=${GOOGLE_BUSINESS_API_KEY:-}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/login')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: Production database (PostgreSQL)
  # db:
  #   image: postgres:15-alpine
  #   container_name: exhibition-marketing-db
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_DB=marketing
  #     - POSTGRES_USER=marketing
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-change-me}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data:
